{
    "version": "2.1.0",
    "description": "Universal ASIO driver (KoordASIO). Non-portable (-np): runs the vendor installer; installs under Program Files system-wide.",
    "homepage": "https://github.com/kormix-io/KoordASIO",
    "license": "MIT",
    "notes": [
        "Package name ends with -np (non-portable): Scoop only downloads and runs the official installer. The app, driver, and Start Menu shortcuts are entirely the vendor’s; there are no Scoop shims.",
        "Prefer an elevated PowerShell if UAC/setup fails. `scoop uninstall` runs the vendor uninstaller under %ProgramFiles%\\KoordASIO so Scoop can drop its install record; the running app is still the normal Windows install."
    ],
    "architecture": {
        "64bit": {
            "url": "https://github.com/kormix-io/KoordASIO/releases/download/r2_1_0/KoordASIO_2.1.0_win64.exe",
            "hash": "6ad650159df381d54054f0f99dee2246a5c4ae610468c39176c7753fe95cffbb"
        }
    },
    "installer": {
        "args": [
            "/VERYSILENT",
            "/SUPPRESSMSGBOXES",
            "/NORESTART"
        ]
    },
    "uninstaller": {
        "script": "$pf = [Environment]::GetFolderPath('ProgramFiles'); $d = Join-Path $pf 'KoordASIO'; $u = Get-ChildItem $d -Filter 'unins*.exe' -ErrorAction SilentlyContinue | Select-Object -First 1; if ($null -ne $u) { Start-Process $u.FullName -ArgumentList '/VERYSILENT','/SUPPRESSMSGBOXES','/NORESTART' -Wait } else { warn 'KoordASIO uninstaller not found; remove manually from Program Files if needed.' }"
    }
}
