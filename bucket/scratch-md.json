{
    "version": "1.0.0",
    "description": "Minimalist, offline-first markdown note-taking app (Tauri).",
    "homepage": "https://www.ericli.io/scratch",
    "license": "MIT",
    "notes": [
        "Portable install: NSIS setup.exe extracted with 7-Zip (no system installer).",
        "Settings stored under %APPDATA%\\com.scratch.app, junctioned to scoop persist.",
        "On Windows 10, install extras/webview2 if the app fails to start."
    ],
    "suggest": {
        "Microsoft Edge WebView2": "extras/webview2"
    },
    "architecture": {
        "64bit": {
            "url": "https://github.com/erictli/scratch/releases/download/v1.0.0/Scratch_1.0.0_x64-setup.exe#/dl.7z",
            "hash": "d240c47f4215e1bab4283c0446402e703776f86ad5d904baab2964a1aee4c968"
        }
    },
    "pre_install": [
        "$links = @(,@(\"$env:APPDATA\\com.scratch.app\", \"$persist_dir\\appdata\\com.scratch.app\"))",
        "foreach ($pair in $links) {",
        "    $linkPath = $pair[0]",
        "    $storePath = $pair[1]",
        "    New-Item -ItemType Directory -Path $storePath -Force | Out-Null",
        "    if (Test-Path -LiteralPath $linkPath) {",
        "        $item = Get-Item -LiteralPath $linkPath -Force",
        "        if ($item.Attributes -band [System.IO.FileAttributes]::ReparsePoint) {",
        "            $targetPath = @($item.Target)[0]",
        "            if ($targetPath) {",
        "                try {",
        "                    $resolvedTarget = (Resolve-Path -LiteralPath $targetPath -ErrorAction Stop).Path",
        "                    $resolvedStore = (Resolve-Path -LiteralPath $storePath -ErrorAction Stop).Path",
        "                    if ($resolvedTarget -ne $resolvedStore) {",
        "                        throw \"Refusing to overwrite existing link at $linkPath because it points to $resolvedTarget\"",
        "                    }",
        "                } catch {",
        "                    throw \"Refusing to overwrite existing link at $linkPath\"",
        "                }",
        "            }",
        "            continue",
        "        }",
        "        Get-ChildItem -LiteralPath $linkPath -Force -ErrorAction SilentlyContinue | ForEach-Object {",
        "            Move-Item -LiteralPath $_.FullName -Destination $storePath -Force -ErrorAction SilentlyContinue",
        "        }",
        "        Remove-Item -LiteralPath $linkPath -Recurse -Force",
        "    }",
        "    New-Item -ItemType Junction -Path $linkPath -Value $storePath | Out-Null",
        "}",
        "Remove-Item \"$dir\\`$PLUGINSDIR\", \"$dir\\uninstall.exe\", \"$dir\\`$*\" -Force -Recurse -ErrorAction SilentlyContinue"
    ],
    "pre_uninstall": [
        "$links = @(,@(\"$env:APPDATA\\com.scratch.app\", \"$persist_dir\\appdata\\com.scratch.app\"))",
        "foreach ($pair in $links) {",
        "    $linkPath = $pair[0]",
        "    $storePath = $pair[1]",
        "    if (!(Test-Path -LiteralPath $linkPath)) { continue }",
        "    $item = Get-Item -LiteralPath $linkPath -Force",
        "    if (!($item.Attributes -band [System.IO.FileAttributes]::ReparsePoint)) { continue }",
        "    $targetPath = @($item.Target)[0]",
        "    if (!$targetPath) { continue }",
        "    try {",
        "        $resolvedTarget = (Resolve-Path -LiteralPath $targetPath -ErrorAction Stop).Path",
        "        $resolvedStore = (Resolve-Path -LiteralPath $storePath -ErrorAction Stop).Path",
        "        if ($resolvedTarget -eq $resolvedStore) {",
        "            Remove-Item -LiteralPath $linkPath -Recurse -Force -ErrorAction SilentlyContinue",
        "        }",
        "    } catch {",
        "        continue",
        "    }",
        "}"
    ],
    "bin": [
        [
            "Scratch.exe",
            "scratch-md"
        ]
    ],
    "shortcuts": [
        [
            "Scratch.exe",
            "Scratch"
        ]
    ],
    "checkver": {
        "github": "https://github.com/erictli/scratch"
    },
    "autoupdate": {
        "architecture": {
            "64bit": {
                "url": "https://github.com/erictli/scratch/releases/download/v$version/Scratch_$version_x64-setup.exe#/dl.7z"
            }
        }
    }
}
